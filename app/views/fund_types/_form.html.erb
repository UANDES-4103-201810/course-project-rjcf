<%= form_with(model: fund_type, local: true) do |form| %>
  <% if fund_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fund_type.errors.count, "error") %> prohibited this fund_type from being saved:</h2>

      <ul>
      <% fund_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :fund_type_type %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :fund_type_amount %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :fund_type_username %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
