<%= form_with(model: admins_project, local: true) do |form| %>
  <% if admins_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_project.errors.count, "error") %> prohibited this admins_project from being saved:</h2>

      <ul>
      <% admins_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
